local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local Camera = workspace.CurrentCamera local Mouse = LocalPlayer:GetMouse() local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local CoreGui = game:GetService("CoreGui") local Lighting = game:GetService("Lighting") local Aiming, FovRadius, MinFov, MaxFov, FovVisible, SmoothAim, Smoothness, TeamCheck, ChamsEnabled, RainbowChams, CustomChamsColor, ChamsTransparency, NamesEnabled, WallCheck = false, 150, 10, 400, true, true, 0.15, true, true, true, Color3.fromRGB(0, 255, 127), 0.5, true, true local MenuVisible, RainbowOverlay, CustomOverlayColor, CapeEnabled, FirefliesEnabled, LanternEnabled = true, true, Color3.fromRGB(0, 255, 127), false, false, false local CurrentVelocityLerp, LanternVelocityLerp = 0, 0 local Connections = {} local Fireflies = {} local AtmosphereMode = 0 local AimPart = "Head" local FovCircle = Drawing.new("Circle") if FovCircle then pcall(function() FovCircle.Visible = FovVisible FovCircle.Radius = FovRadius FovCircle.Filled = false FovCircle.Color = Color3.fromRGB(0, 255, 127) FovCircle.Thickness = 1 FovCircle.Transparency = 0.7 end) end local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "TWKSOverlayFixed" ScreenGui.ResetOnSpawn = false ScreenGui.DisplayOrder = 999999 pcall(function() ScreenGui.Parent = CoreGui end) if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end local NotificationFrame = Instance.new("Frame") NotificationFrame.Size = UDim2.new(0, 320, 0, 80) NotificationFrame.Position = UDim2.new(0.5, -160, -0.2, 0) NotificationFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15) NotificationFrame.BorderSizePixel = 0 NotificationFrame.ZIndex = 1000000 NotificationFrame.BackgroundTransparency = 1 NotificationFrame.Parent = ScreenGui local NotifCorner = Instance.new("UICorner") NotifCorner.CornerRadius = UDim.new(0, 12) NotifCorner.Parent = NotificationFrame local NotifStroke = Instance.new("UIStroke") NotifStroke.Thickness = 2 NotifStroke.Transparency = 1 NotifStroke.Parent = NotificationFrame local NotifText = Instance.new("TextLabel") NotifText.Size = UDim2.new(1, 0, 1, 0) NotifText.Position = UDim2.new(0, 0, 0, 0) NotifText.BackgroundTransparency = 1 NotifText.Text = "AimScript\nMade by v1stanov" NotifText.TextSize = 22 NotifText.Font = Enum.Font.GothamBold NotifText.TextXAlignment = Enum.TextXAlignment.Center NotifText.TextYAlignment = Enum.TextYAlignment.Center NotifText.TextTransparency = 1 NotifText.ZIndex = 1000001 NotifText.Parent = NotificationFrame local LoadingBg = Instance.new("Frame") LoadingBg.Size = UDim2.new(0, 200, 0, 6) LoadingBg.Position = UDim2.new(0.5, -100, 0.5, 55) LoadingBg.BackgroundColor3 = Color3.fromRGB(30, 30, 30) LoadingBg.BorderSizePixel = 0 LoadingBg.ZIndex = 1000002 LoadingBg.BackgroundTransparency = 1 LoadingBg.Parent = ScreenGui local LoadingFill = Instance.new("Frame") LoadingFill.Size = UDim2.new(0, 0, 1, 0) LoadingFill.BackgroundColor3 = Color3.fromRGB(0, 255, 127) LoadingFill.BorderSizePixel = 0 LoadingFill.ZIndex = 1000003 LoadingFill.Parent = LoadingBg local LoadingFillCorner = Instance.new("UICorner") LoadingFillCorner.CornerRadius = UDim.new(1, 0) LoadingFillCorner.Parent = LoadingFill local MainFrame = Instance.new("Frame") MainFrame.Size = UDim2.new(0, 220, 0, 595) MainFrame.Position = UDim2.new(0.5, -110, 0.5, -297) MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20) MainFrame.BorderSizePixel = 0 MainFrame.Active = true MainFrame.ClipsDescendants = false MainFrame.Visible = false MainFrame.Parent = ScreenGui local UICorner = Instance.new("UICorner") UICorner.CornerRadius = UDim.new(0, 8) UICorner.Parent = MainFrame local MenuStroke = Instance.new("UIStroke") MenuStroke.Thickness = 2 MenuStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border MenuStroke.Parent = MainFrame local Line = Instance.new("Frame") Line.Size = UDim2.new(1, 0, 0, 4) Line.BackgroundColor3 = Color3.fromRGB(0, 255, 127) Line.BorderSizePixel = 0 Line.Parent = MainFrame local Title = Instance.new("TextLabel") Title.Size = UDim2.new(1, -30, 0, 30) Title.Position = UDim2.new(0, 0, 0, 5) Title.Text = "  AimScript - v1stanov" Title.TextColor3 = Color3.fromRGB(255, 255, 255) Title.TextSize = 14 Title.Font = Enum.Font.GothamBold Title.TextXAlignment = Enum.TextXAlignment.Left Title.BackgroundTransparency = 1 Title.Parent = MainFrame local DummyFrame = Instance.new("Frame") DummyFrame.Size = UDim2.new(0, 140, 0, 240) DummyFrame.Position = UDim2.new(1, 10, 0, 0) DummyFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20) DummyFrame.BorderSizePixel = 0 DummyFrame.Visible = false DummyFrame.Parent = MainFrame local DummyCorner = Instance.new("UICorner") DummyCorner.CornerRadius = UDim.new(0, 8) DummyCorner.Parent = DummyFrame local DummyLine = Instance.new("Frame") DummyLine.Size = UDim2.new(1, 0, 0, 4) DummyLine.BackgroundColor3 = Color3.fromRGB(0, 255, 127) DummyLine.BorderSizePixel = 0 DummyLine.Parent = DummyFrame local DummyTitle = Instance.new("TextLabel") DummyTitle.Size = UDim2.new(1, 0, 0, 30) DummyTitle.Position = UDim2.new(0, 0, 0, 5) DummyTitle.Text = "ИНТЕРАКТИВНЫЙ DUMMY" DummyTitle.TextColor3 = Color3.fromRGB(255, 255, 255) DummyTitle.TextSize = 10 DummyTitle.Font = Enum.Font.GothamBold DummyTitle.BackgroundTransparency = 1 DummyTitle.Parent = DummyFrame local DummyContainer = Instance.new("Frame") DummyContainer.Size = UDim2.new(1, -20, 1, -45) DummyContainer.Position = UDim2.new(0, 10, 0, 35) DummyContainer.BackgroundTransparency = 1 DummyContainer.Parent = DummyFrame local dummyButtons = {} local function makeDummyPartBtn(text, size, pos, name) local b = Instance.new("TextButton") b.Size = size b.Position = pos b.BackgroundColor3 = Color3.fromRGB(30, 30, 30) b.Text = text b.TextColor3 = Color3.fromRGB(200, 200, 200) b.Font = Enum.Font.GothamBold b.TextSize = 9 b.Parent = DummyContainer local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0, 4) c.Parent = b b.MouseButton1Click:Connect(function() AimPart = name end) dummyButtons[name] = b return b end local D_Head = makeDummyPartBtn("H", UDim2.new(0, 30, 0, 30), UDim2.new(0.5, -15, 0, 5), "Head") local D_Torso = makeDummyPartBtn("TORSO", UDim2.new(0, 50, 0, 60), UDim2.new(0.5, -25, 0, 40), "Torso") local D_LArm = makeDummyPartBtn("L", UDim2.new(0, 25, 0, 60), UDim2.new(0.5, -55, 0, 40), "LeftArm") local D_RArm = makeDummyPartBtn("R", UDim2.new(0, 25, 0, 60), UDim2.new(0.5, 30, 0, 40), "RightArm") local D_LLeg = makeDummyPartBtn("LL", UDim2.new(0, 22, 0, 55), UDim2.new(0.5, -25, 0, 105), "LeftLeg") local D_RLeg = makeDummyPartBtn("RL", UDim2.new(0, 22, 0, 55), UDim2.new(0.5, 3, 0, 105), "RightLeg") local D_Random = makeDummyPartBtn("РАНДОМ", UDim2.new(1, 0, 0, 25), UDim2.new(0, 0, 0, 165), "Random") local function updateTargetUI() for name, button in pairs(dummyButtons) do if name == AimPart then button.BackgroundColor3 = Color3.fromRGB(35, 35, 35) button.TextColor3 = CustomOverlayColor else button.BackgroundColor3 = Color3.fromRGB(25, 25, 25) button.TextColor3 = Color3.fromRGB(150, 150, 150) end end end task.spawn(function() local infoIn = TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out) TweenService:Create(NotificationFrame, infoIn, {Position = UDim2.new(0.5, -160, 0.4, -40), BackgroundTransparency = 0.05}):Play() TweenService:Create(NotifText, infoIn, {TextTransparency = 0}):Play() TweenService:Create(NotifStroke, infoIn, {Transparency = 0}):Play() TweenService:Create(LoadingBg, infoIn, {BackgroundTransparency = 0}):Play() task.wait(0.6) local loadTween = TweenService:Create(LoadingFill, TweenInfo.new(12, Enum.EasingStyle.Linear), {Size = UDim2.new(1, 0, 1, 0)}) loadTween:Play() loadTween.Completed:Wait() task.wait(0.5) local infoOut = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.In) TweenService:Create(NotificationFrame, infoOut, {Position = UDim2.new(0.5, -160, 1.2, 0), BackgroundTransparency = 1}):Play() TweenService:Create(NotifText, infoOut, {TextTransparency = 1}):Play() TweenService:Create(NotifStroke, infoOut, {Transparency = 1}):Play() TweenService:Create(LoadingBg, infoOut, {BackgroundTransparency = 1}):Play() TweenService:Create(LoadingFill, infoOut, {BackgroundTransparency = 1}):Play() task.wait(0.5) NotificationFrame:Destroy() LoadingBg:Destroy() MainFrame.Visible = true DummyFrame.Visible = true end) local CloseBtn = Instance.new("TextButton") CloseBtn.Size = UDim2.new(0, 20, 0, 20) CloseBtn.Position = UDim2.new(1, -25, 0, 10) CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50) CloseBtn.Text = "X" CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255) CloseBtn.Font = Enum.Font.GothamBold CloseBtn.TextSize = 12 CloseBtn.Parent = MainFrame local CloseCorner = Instance.new("UICorner") CloseCorner.CornerRadius = UDim.new(1, 0) CloseCorner.Parent = CloseBtn local function UnloadCheat() FovVisible = false Aiming = false ChamsEnabled = false CapeEnabled = false FirefliesEnabled = false LanternEnabled = false pcall(function() local sky = Lighting:FindFirstChildOfClass("Sky") if sky then sky:Destroy() end Lighting.Ambient = Color3.fromRGB(128, 128, 128) Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128) Lighting.ClockTime = 14 Lighting.Brightness = 2 Lighting.FogEnd = 100000 end) if LocalPlayer.Character then local f1 = LocalPlayer.Character:FindFirstChild("TWKS_CapeFolder") if f1 then f1:Destroy() end local f2 = LocalPlayer.Character:FindFirstChild("TWKS_Fireflies") if f2 then f2:Destroy() end local f3 = LocalPlayer.Character:FindFirstChild("TWKS_LanternFolder") if f3 then f3:Destroy() end end Fireflies = {} for _, conn in pairs(Connections) do pcall(function() conn:Disconnect() end) end pcall(function() pcall(function() if FovCircle then FovCircle:Destroy() end end) ScreenGui:Destroy() end) for _, p in ipairs(Players:GetPlayers()) do pcall(function() if p.Character then local h = p.Character:FindFirstChild("TWKS_Highlight")if h then h:Destroy() end local b = p.Character:FindFirstChild("TWKS_NameESP") if b then b:Destroy() end end end) end end CloseBtn.MouseButton1Click:Connect(UnloadCheat) local Status = Instance.new("TextLabel") Status.Size = UDim2.new(1, 0, 0, 25) Status.Position = UDim2.new(0, 0, 0, 35) Status.Text = "  AIMBOT: READY (RMB)" Status.TextColor3 = Color3.fromRGB(150, 150, 150) Status.TextSize = 12 Status.Font = Enum.Font.Gotham Status.TextXAlignment = Enum.TextXAlignment.Left Status.BackgroundTransparency = 1 Status.Parent = MainFrame local CatAim = Instance.new("TextLabel") CatAim.Size = UDim2.new(1, -20, 0, 20) CatAim.Position = UDim2.new(0, 10, 0, 65) CatAim.Text = "--- AIMBOT ---" CatAim.TextColor3 = Color3.fromRGB(120, 120, 120) CatAim.TextSize = 10 CatAim.Font = Enum.Font.GothamBold CatAim.BackgroundTransparency = 1 CatAim.Parent = MainFrame local FovStatus = Instance.new("TextLabel") FovStatus.Size = UDim2.new(1, 0, 0, 20) FovStatus.Position = UDim2.new(0, 0, 0, 85) FovStatus.Text = "  FOV RADIUS: " .. tostring(FovRadius) .. "px" FovStatus.TextColor3 = Color3.fromRGB(180, 180, 180) FovStatus.TextSize = 11 FovStatus.Font = Enum.Font.Gotham FovStatus.TextXAlignment = Enum.TextXAlignment.Left FovStatus.BackgroundTransparency = 1 FovStatus.Parent = MainFrame local SliderTrack = Instance.new("Frame") SliderTrack.Size = UDim2.new(0, 180, 0, 6) SliderTrack.Position = UDim2.new(0, 20, 0, 110) SliderTrack.BackgroundColor3 = Color3.fromRGB(45, 45, 45) SliderTrack.BorderSizePixel = 0 SliderTrack.Parent = MainFrame local SliderTrackCorner = Instance.new("UICorner") SliderTrackCorner.CornerRadius = UDim.new(1, 0) SliderTrackCorner.Parent = SliderTrack local SliderFill = Instance.new("Frame") SliderFill.BackgroundColor3 = Color3.fromRGB(0, 255, 127) SliderFill.BorderSizePixel = 0 SliderFill.Parent = SliderTrack local SliderFillCorner = Instance.new("UICorner") SliderFillCorner.CornerRadius = UDim.new(1, 0) SliderFillCorner.Parent = SliderFill local SliderBtn = Instance.new("TextButton") SliderBtn.Size = UDim2.new(0, 12, 0, 12) SliderBtn.Position = UDim2.new(0, 0, 0.5, -6) SliderBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255) SliderBtn.Text = "" SliderBtn.BorderSizePixel = 0 SliderBtn.Parent = SliderTrack local SliderBtnCorner = Instance.new("UICorner") SliderBtnCorner.CornerRadius = UDim.new(1, 0) SliderBtnCorner.Parent = SliderBtn local SmoothBtn = Instance.new("TextButton") SmoothBtn.Size = UDim2.new(0, 85, 0, 25) SmoothBtn.Position = UDim2.new(0, 20, 0, 125) SmoothBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) SmoothBtn.Text = "Плавная" SmoothBtn.TextColor3 = Color3.fromRGB(0, 255, 127) SmoothBtn.Font = Enum.Font.GothamBold SmoothBtn.TextSize = 11 SmoothBtn.Parent = MainFrame local SmoothCorner = Instance.new("UICorner") SmoothCorner.CornerRadius = UDim.new(0, 4) SmoothCorner.Parent = SmoothBtn local InstantBtn = Instance.new("TextButton") InstantBtn.Size = UDim2.new(0, 85, 0, 25) InstantBtn.Position = UDim2.new(0, 115, 0, 125) InstantBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) InstantBtn.Text = "Резкая" InstantBtn.TextColor3 = Color3.fromRGB(150, 150, 150) InstantBtn.Font = Enum.Font.GothamBold InstantBtn.TextSize = 11 InstantBtn.Parent = MainFrame local InstantCorner = Instance.new("UICorner") InstantCorner.CornerRadius = UDim.new(0, 4) InstantCorner.Parent = InstantBtn local TeamBtn = Instance.new("TextButton") TeamBtn.Size = UDim2.new(0, 180, 0, 25) TeamBtn.Position = UDim2.new(0, 20, 0, 155) TeamBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) TeamBtn.Text = "Проверка Команды: ВКЛ" TeamBtn.TextColor3 = Color3.fromRGB(0, 255, 127) TeamBtn.Font = Enum.Font.GothamBold TeamBtn.TextSize = 11 TeamBtn.Parent = MainFrame local TeamCorner = Instance.new("UICorner") TeamCorner.CornerRadius = UDim.new(0, 4) TeamCorner.Parent = TeamBtn local WallBtn = Instance.new("TextButton") WallBtn.Size = UDim2.new(0, 180, 0, 25) WallBtn.Position = UDim2.new(0, 20, 0, 185) WallBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) WallBtn.Text = "Проверка Стен: ВКЛ" WallBtn.TextColor3 = Color3.fromRGB(0, 255, 127) WallBtn.Font = Enum.Font.GothamBold WallBtn.TextSize = 11 WallBtn.Parent = MainFrame local WallCorner = Instance.new("UICorner") WallCorner.CornerRadius = UDim.new(0, 4) WallCorner.Parent = WallBtn local CatVisuals = Instance.new("TextLabel") CatVisuals.Size = UDim2.new(1, -20, 0, 20) CatVisuals.Position = UDim2.new(0, 10, 0, 220) CatVisuals.Text = "--- VISUALS & ESP ---" CatVisuals.TextColor3 = Color3.fromRGB(120, 120, 120) CatVisuals.TextSize = 10 CatVisuals.Font = Enum.Font.GothamBold CatVisuals.BackgroundTransparency = 1 CatVisuals.Parent = MainFrame local ChamsBtn = Instance.new("TextButton") ChamsBtn.Size = UDim2.new(0, 180, 0, 25) ChamsBtn.Position = UDim2.new(0, 20, 0, 240) ChamsBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) ChamsBtn.Text = "Чамсы (Wallhack): ВКЛ" ChamsBtn.TextColor3 = Color3.fromRGB(0, 255, 127) ChamsBtn.Font = Enum.Font.GothamBold ChamsBtn.TextSize = 11 ChamsBtn.Parent = MainFrame local ChamsCorner = Instance.new("UICorner") ChamsCorner.CornerRadius = UDim.new(0, 4) ChamsCorner.Parent = ChamsBtn local ColorModeBtn = Instance.new("TextButton") ColorModeBtn.Size = UDim2.new(0, 180, 0, 25) ColorModeBtn.Position = UDim2.new(0, 20, 0, 270) ColorModeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) ColorModeBtn.Text = "Режим Чамсов: РАДУГА" ColorModeBtn.TextColor3 = Color3.fromRGB(0, 255, 127) ColorModeBtn.Font = Enum.Font.GothamBold ColorModeBtn.TextSize = 11 ColorModeBtn.Parent = MainFrame local ColorModeCorner = Instance.new("UICorner") ColorModeCorner.CornerRadius = UDim.new(0, 4) ColorModeCorner.Parent = ColorModeBtn local CatCosmetics = Instance.new("TextLabel") CatCosmetics.Size = UDim2.new(1, -20, 0, 20) CatCosmetics.Position = UDim2.new(0, 10, 0, 305) CatCosmetics.Text = "--- COSMETICS ---" CatCosmetics.TextColor3 = Color3.fromRGB(120, 120, 120) CatCosmetics.TextSize = 10 CatCosmetics.Font = Enum.Font.GothamBold CatCosmetics.BackgroundTransparency = 1 CatCosmetics.Parent = MainFrame local CapeBtn = Instance.new("TextButton") CapeBtn.Size = UDim2.new(0, 180, 0, 25) CapeBtn.Position = UDim2.new(0, 20, 0, 325) CapeBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) CapeBtn.Text = "Радужный Плащ: ВЫКЛ" CapeBtn.TextColor3 = Color3.fromRGB(150, 150, 150) CapeBtn.Font = Enum.Font.GothamBold CapeBtn.TextSize = 11 CapeBtn.Parent = MainFrame local CapeCorner = Instance.new("UICorner") CapeCorner.CornerRadius = UDim.new(0, 4) CapeCorner.Parent = CapeBtn local FirefliesBtn = Instance.new("TextButton") FirefliesBtn.Size = UDim2.new(0, 180, 0, 25) FirefliesBtn.Position = UDim2.new(0, 20, 0, 355) FirefliesBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) FirefliesBtn.Text = "Светлячки: ВЫКЛ" FirefliesBtn.TextColor3 = Color3.fromRGB(150, 150, 150) FirefliesBtn.Font = Enum.Font.GothamBold FirefliesBtn.TextSize = 11 FirefliesBtn.Parent = MainFrame local FirefliesCorner = Instance.new("UICorner") FirefliesCorner.CornerRadius = UDim.new(0, 4) FirefliesCorner.Parent = FirefliesBtn local LanternBtn = Instance.new("TextButton") LanternBtn.Size = UDim2.new(0, 180, 0, 25) LanternBtn.Position = UDim2.new(0, 20, 0, 385) LanternBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) LanternBtn.Text = "Радужный Фонарь: ВЫКЛ" LanternBtn.TextColor3 = Color3.fromRGB(150, 150, 150) LanternBtn.Font = Enum.Font.GothamBold LanternBtn.TextSize = 11 LanternBtn.Parent = MainFrame local LanternCorner = Instance.new("UICorner") LanternCorner.CornerRadius = UDim.new(0, 4) LanternCorner.Parent = LanternBtn local CatSettings = Instance.new("TextLabel") CatSettings.Size = UDim2.new(1, -20, 0, 20) CatSettings.Position = UDim2.new(0, 10, 0, 420) CatSettings.Text = "--- SETTINGS ---" CatSettings.TextColor3 = Color3.fromRGB(120, 120, 120) CatSettings.TextSize = 10 CatSettings.Font = Enum.Font.GothamBold CatSettings.BackgroundTransparency = 1 CatSettings.Parent = MainFrame local OverlayColorBtn = Instance.new("TextButton") OverlayColorBtn.Size = UDim2.new(0, 180, 0, 25) OverlayColorBtn.Position = UDim2.new(0, 20, 0, 440) OverlayColorBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) OverlayColorBtn.Text = "Цвет Меню: РАДУГА" OverlayColorBtn.TextColor3 = Color3.fromRGB(0, 255, 127) OverlayColorBtn.Font = Enum.Font.GothamBold OverlayColorBtn.TextSize = 11 OverlayColorBtn.Parent = MainFrame local OverlayColorCorner = Instance.new("UICorner") OverlayColorCorner.CornerRadius = UDim.new(0, 4) OverlayColorCorner.Parent = OverlayColorBtn local UnloadBtn = Instance.new("TextButton") UnloadBtn.Size = UDim2.new(0, 180, 0, 25) UnloadBtn.Position = UDim2.new(0, 20, 0, 470) UnloadBtn.BackgroundColor3 = Color3.fromRGB(40, 20, 20) UnloadBtn.Text = "ПОЛНОЕ ЗАКРЫТИЕ ЧИТА" UnloadBtn.TextColor3 = Color3.fromRGB(255, 100, 100) UnloadBtn.Font = Enum.Font.GothamBold UnloadBtn.TextSize = 11 UnloadBtn.Parent = MainFrame local UnloadCorner = Instance.new("UICorner") UnloadCorner.CornerRadius = UDim.new(0, 4) UnloadCorner.Parent = UnloadBtn UnloadBtn.MouseButton1Click:Connect(UnloadCheat) local SliderActive = false local function updateSlider() local mouseX = UserInputService:GetMouseLocation().X local trackLeftX = SliderTrack.AbsolutePosition.X local scale = math.clamp((mouseX - trackLeftX) / SliderTrack.AbsoluteSize.X, 0, 1) SliderBtn.Position = UDim2.new(scale, -6, 0.5, -6) SliderFill.Size = UDim2.new(scale, 0, 1, 0) FovRadius = math.round(MinFov + (scale * (MaxFov - MinFov))) pcall(function() if FovCircle then FovCircle.Radius = FovRadius end end) FovStatus.Text = "  FOV RADIUS: " .. tostring(FovRadius) .. "px" end Connections.SliderDown = SliderBtn.MouseButton1Down:Connect(function() SliderActive = true end) Connections.SliderUp = UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then SliderActive = false end end) Connections.SliderMove = UserInputService.InputChanged:Connect(function(input) if SliderActive and input.UserInputType == Enum.UserInputType.MouseMovement then updateSlider() end end) Connections.SmoothClick = SmoothBtn.MouseButton1Click:Connect(function() SmoothAim = true SmoothBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) SmoothBtn.TextColor3 = CustomOverlayColor InstantBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) InstantBtn.TextColor3 = Color3.fromRGB(150, 150, 150) end) Connections.InstantClick = InstantBtn.MouseButton1Click:Connect(function() SmoothAim = false InstantBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) InstantBtn.TextColor3 = CustomOverlayColor SmoothBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) SmoothBtn.TextColor3 = Color3.fromRGB(150, 150, 150) end) Connections.TeamClick = TeamBtn.MouseButton1Click:Connect(function() TeamCheck = not TeamCheck TeamBtn.Text = TeamCheck and "Проверка Команды: ВКЛ" or "Проверка Команды: ВЫКЛ" TeamBtn.BackgroundColor3 = TeamCheck and Color3.fromRGB(35, 35, 35) or Color3.fromRGB(25, 25, 25) TeamBtn.TextColor3 = TeamCheck and CustomOverlayColor or Color3.fromRGB(150, 150, 150) end) Connections.WallClick = WallBtn.MouseButton1Click:Connect(function() WallCheck = not WallCheck WallBtn.Text = WallCheck and "Проверка Стен: ВКЛ" or "Проверка Стен: ВЫКЛ" WallBtn.BackgroundColor3 = WallCheck and Color3.fromRGB(35, 35, 35) or Color3.fromRGB(25, 25, 25) WallBtn.TextColor3 = WallCheck and CustomOverlayColor or Color3.fromRGB(150, 150, 150) end) Connections.ChamsClick = ChamsBtn.MouseButton1Click:Connect(function() ChamsEnabled = not ChamsEnabled ChamsBtn.Text = ChamsEnabled and "Чамсы (Wallhack): ВКЛ" or "Чамсы (Wallhack): ВЫКЛ" ChamsBtn.BackgroundColor3 = ChamsEnabled and Color3.fromRGB(35, 35, 35) or Color3.fromRGB(25, 25, 25) ChamsBtn.TextColor3 = ChamsEnabled and CustomOverlayColor or Color3.fromRGB(150, 150, 150) end) Connections.ColorModeClick = ColorModeBtn.MouseButton1Click:Connect(function() RainbowChams = not RainbowChams ColorModeBtn.Text = RainbowChams and "Режим Чамсов: РАДУГА" or "Режим Чамсов: СТАТИКА" end) Connections.OverlayColorClick = OverlayColorBtn.MouseButton1Click:Connect(function() RainbowOverlay = not RainbowOverlay if RainbowOverlay then OverlayColorBtn.Text = "Цвет Меню: РАДУГА" else OverlayColorBtn.Text = "Цвет Меню: ЗЕЛЕНЫЙ" CustomOverlayColor = Color3.fromRGB(0, 255, 127) Line.BackgroundColor3 = CustomOverlayColor pcall(function() if FovCircle then FovCircle.Color = CustomOverlayColor end end) SliderFill.BackgroundColor3 = CustomOverlayColor if SmoothAim then SmoothBtn.TextColor3 = CustomOverlayColor end if TeamCheck then TeamBtn.TextColor3 = CustomOverlayColor end if WallCheck then WallBtn.TextColor3 = CustomOverlayColor end if ChamsEnabled then ChamsBtn.TextColor3 = CustomOverlayColor end if RainbowChams then ColorModeBtn.TextColor3 = CustomOverlayColor end OverlayColorBtn.TextColor3 = CustomOverlayColor if FirefliesEnabled then FirefliesBtn.TextColor3 = CustomOverlayColor end if LanternEnabled then LanternBtn.TextColor3 = CustomOverlayColor end updateTargetUI() end end) Connections.CapeClick = CapeBtn.MouseButton1Click:Connect(function() CapeEnabled = not CapeEnabled if CapeEnabled then CapeBtn.Text = "Радужный Плащ: ВКЛ" CapeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) CapeBtn.TextColor3 = CustomOverlayColor else CapeBtn.Text = "Радужный Плащ: ВЫКЛ" CapeBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) CapeBtn.TextColor3 = Color3.fromRGB(150, 150, 150) if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_CapeFolder") if f then f:Destroy() end end end end) Connections.FirefliesClick = FirefliesBtn.MouseButton1Click:Connect(function() FirefliesEnabled = not FirefliesEnabled if FirefliesEnabled then FirefliesBtn.Text = "Светлячки: ВКЛ" FirefliesBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) FirefliesBtn.TextColor3 = CustomOverlayColor else FirefliesBtn.Text = "Светлячки: ВЫКЛ" FirefliesBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) FirefliesBtn.TextColor3 = Color3.fromRGB(150, 150, 150) if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_Fireflies") if f then f:Destroy() end end Fireflies = {} end end) Connections.LanternClick = LanternBtn.MouseButton1Click:Connect(function() LanternEnabled = not LanternEnabled if LanternEnabled then LanternBtn.Text = "Радужный Фонарь: ВКЛ" LanternBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35) LanternBtn.TextColor3 = CustomOverlayColor else LanternBtn.Text = "Радужный Фонарь: ВЫКЛ" LanternBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) LanternBtn.TextColor3 = Color3.fromRGB(150, 150, 150) if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_LanternFolder") if f then f:Destroy() end end end end) local AtmosBtn = Instance.new("TextButton") AtmosBtn.Size = UDim2.new(0, 180, 0, 25) AtmosBtn.Position = UDim2.new(0, 20, 0, 500) AtmosBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25) AtmosBtn.Text = "Атмосфера: ДЕФОЛТ" AtmosBtn.TextColor3 = Color3.fromRGB(150, 150, 150) AtmosBtn.Font = Enum.Font.GothamBold AtmosBtn.TextSize = 11 AtmosBtn.Parent = MainFrame local AtmosCorner = Instance.new("UICorner") AtmosCorner.CornerRadius = UDim.new(0, 4) AtmosCorner.Parent = AtmosBtn local function updateSky(skyColor) local sky = Lighting:FindFirstChildOfClass("Sky") if not sky then sky = Instance.new("Sky") sky.Parent = Lighting end sky.SkyboxBk = "" sky.SkyboxDn = "" sky.SkyboxFt = "" sky.SkyboxLf = "" sky.SkyboxRt = "" sky.SkyboxUp = "" sky.CelestialBodiesShown = (skyColor == nil) if skyColor then sky.SkyboxBk = "rbxassetid://101452924147043" sky.SkyboxDn = "rbxassetid://101452924147043" sky.SkyboxFt = "rbxassetid://101452924147043" sky.SkyboxLf = "rbxassetid://101452924147043" sky.SkyboxRt = "rbxassetid://101452924147043" sky.SkyboxUp = "rbxassetid://101452924147043" end end Connections.AtmosClick = AtmosBtn.MouseButton1Click:Connect(function() AtmosphereMode = (AtmosphereMode + 1) % 4 if AtmosphereMode == 0 then AtmosBtn.Text = "Атмосфера: ДЕФОЛТ" Lighting.Ambient = Color3.fromRGB(128, 128, 128) Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128) Lighting.ClockTime = 14 Lighting.Brightness = 2 Lighting.FogEnd = 100000 updateSky(nil) elseif AtmosphereMode == 1 then AtmosBtn.Text = "Атмосфера: НОЧЬ" Lighting.Ambient = Color3.fromRGB(15, 15, 30) Lighting.OutdoorAmbient = Color3.fromRGB(15, 15, 30) Lighting.ClockTime = 0 Lighting.Brightness = 0.5 Lighting.FogEnd = 100000 updateSky(Color3.fromRGB(5, 5, 15)) elseif AtmosphereMode == 2 then AtmosBtn.Text = "Атмосфера: ПОДЗЕМЕЛЬЕ" Lighting.Ambient = Color3.fromRGB(40, 10, 10) Lighting.OutdoorAmbient = Color3.fromRGB(10, 5, 5) Lighting.ClockTime = 20 Lighting.Brightness = 0.8 Lighting.FogEnd = 100000 updateSky(Color3.fromRGB(30, 0, 0)) elseif AtmosphereMode == 3 then AtmosBtn.Text = "Атмосфера: КИСЛОТА" Lighting.Ambient = Color3.fromRGB(0, 60, 20) Lighting.OutdoorAmbient = Color3.fromRGB(0, 80, 10) Lighting.ClockTime = 12 Lighting.Brightness = 3.5 Lighting.FogEnd = 100000 updateSky(Color3.fromRGB(0, 40, 10)) end end) local ToggleInfoLabel = Instance.new("TextLabel") ToggleInfoLabel.Size = UDim2.new(0, 180, 0, 20) ToggleInfoLabel.Position = UDim2.new(0, 20, 0, 535) ToggleInfoLabel.BackgroundTransparency = 1 ToggleInfoLabel.Text = "[INSERT] скрыть/показать меню" ToggleInfoLabel.TextColor3 = Color3.fromRGB(130, 130, 130) ToggleInfoLabel.TextSize = 10 ToggleInfoLabel.Font = Enum.Font.Gotham ToggleInfoLabel.Parent = MainFrame local Dragging, DragInput, DragStart, StartPosition Connections.DragStart = MainFrame.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then Dragging = true DragStart = input.Position StartPosition = MainFrame.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then Dragging = false end end) end end) Connections.DragMove = MainFrame.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then DragInput = input end end) Connections.DragUpdate = UserInputService.InputChanged:Connect(function(input) if input == DragInput and Dragging then local delta = input.Position - DragStart MainFrame.Position = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + delta.Y) end end) Connections.ToggleKey = UserInputService.InputBegan:Connect(function(input, processed) if not processed and input.KeyCode == Enum.KeyCode.Insert then MenuVisible = not MenuVisible MainFrame.Visible = MenuVisible DummyFrame.Visible = (MenuVisible and MainFrame.Visible) end end) local function applyESP(player) if player == LocalPlayer then return end local function setupChar(char) pcall(function() local oldH = char:FindFirstChild("TWKS_Highlight") if oldH then oldH:Destroy() end local oldB = char:FindFirstChild("TWKS_NameESP") if oldB then oldB:Destroy() end local Highlight = Instance.new("Highlight") Highlight.Name = "TWKS_Highlight" Highlight.FillTransparency = ChamsTransparency Highlight.OutlineTransparency = 0.2 Highlight.Parent = char Highlight.Adornee = char Highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop local Head = char:WaitForChild("Head", 5) if Head then local Billboard = Instance.new("BillboardGui") Billboard.Name = "TWKS_NameESP" Billboard.Adornee = Head Billboard.Size = UDim2.new(0, 100, 0, 20) Billboard.StudsOffset = Vector3.new(0, 2.5, 0) Billboard.AlwaysOnTop = true local NameLabel = Instance.new("TextLabel") NameLabel.Size = UDim2.new(1, 0, 1, 0) NameLabel.BackgroundTransparency = 1 NameLabel.Text = player.Name NameLabel.TextColor3 = Color3.fromRGB(255, 255, 255) NameLabel.TextSize = 10 NameLabel.Font = Enum.Font.GothamBold NameLabel.TextStrokeTransparency = 0.5 NameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) NameLabel.Parent = Billboard Billboard.Parent = char end end) end if player.Character then setupChar(player.Character) end player.CharacterAdded:Connect(setupChar) end for _, p in ipairs(Players:GetPlayers()) do pcall(applyESP, p) end Connections.PlayerAdded = Players.PlayerAdded:Connect(applyESP) local function isVisible(targetPart) if not WallCheck then return true end local ignoreList = {LocalPlayer.Character, Camera} local params = RaycastParams.new() params.FilterDescendantsInstances = ignoreList params.FilterType = Enum.RaycastFilterType.Exclude local result = workspace:Raycast(Camera.CFrame.Position, targetPart.Position - Camera.CFrame.Position, params) if result and result.Instance:IsDescendantOf(targetPart.Parent) then return true end return false end local function getClosestPlayer() local closestPlayer, shortestDistance, mousePos = nil, math.huge, Vector2.new(Mouse.X, Mouse.Y) for _, player in ipairs(Players:GetPlayers()) do if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") and player.Character:FindFirstChildOfClass("Humanoid") then if not TeamCheck or player.Team ~= LocalPlayer.Team then if player.Character:FindFirstChildOfClass("Humanoid").Health > 0 and isVisible(player.Character.Head) then local headPos, onScreen = Camera:WorldToViewportPoint(player.Character.Head.Position) if onScreen then local distance = (Vector2.new(headPos.X, headPos.Y) - mousePos).Magnitude if distance < shortestDistance and distance <= FovRadius then closestPlayer = player shortestDistance = distance end end end end end end return closestPlayer end local function DrawCape(char, color) local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") if not torso then return end local folder = char:FindFirstChild("TWKS_CapeFolder") if not folder then folder = Instance.new("Folder") folder.Name = "TWKS_CapeFolder" folder.Parent = char local segmentCount = 6 local lastSegment = torso for i = 1, segmentCount do local segment = Instance.new("Part") segment.Name = "Segment" .. i segment.Size = Vector3.new(1.8 - (i * 0.1), 0.5, 0.05) segment.CanCollide = false segment.Material = Enum.Material.Neon segment.Parent = folder local motor = Instance.new("Motor", segment) motor.Name = "Joint" motor.Part0 = lastSegment motor.Part1 = segment if i == 1 then motor.C0 = CFrame.new(0, 1, 0.55) * CFrame.Angles(0, math.rad(180), 0) motor.C1 = CFrame.new(0, 0.25, 0) else motor.C0 = CFrame.new(0, -0.25, 0) motor.C1 = CFrame.new(0, 0.25, 0) end lastSegment = segment end end local targetVelocity = torso.AssemblyLinearVelocity.Magnitude CurrentVelocityLerp = CurrentVelocityLerp + (targetVelocity - CurrentVelocityLerp) * 0.08 local children = folder:GetChildren() table.sort(children, function(a, b) return a.Name < b.Name end) for i, segment in ipairs(children) do segment.Color = color local joint = segment:FindFirstChild("Joint") if joint then local angleX = math.sin(tick() * 6 + (i * 0.4)) * 2 * (CurrentVelocityLerp > 1 and 1 or 0) local waveY = math.cos(tick() * 4) * 1.5 if i == 1 then joint.C0 = CFrame.new(0, 1, 0.55) * CFrame.Angles(math.rad(-12 - (CurrentVelocityLerp * 1.4) + waveY), math.rad(180), math.rad(angleX)) else joint.C0 = CFrame.new(0, -0.25, 0) * CFrame.Angles(math.rad(8 + (CurrentVelocityLerp * 0.7) + waveY), 0, math.rad(angleX)) end end end end local function UpdateFireflies(char, color) local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") if not torso then return end local folder = char:FindFirstChild("TWKS_Fireflies") if not folder then folder = Instance.new("Folder") folder.Name = "TWKS_Fireflies" folder.Parent = char local fireflyCount = 12 for i = 1, fireflyCount do local p = Instance.new("Part") p.Name = "Firefly" p.Size = Vector3.new(0.15, 0.15, 0.15) p.CanCollide = false p.Anchored = true p.Material = Enum.Material.Neon p.Shape = Enum.PartType.Ball p.Parent = folder table.insert(Fireflies, {Part = p, Offset = math.random() * math.pi * 2, Height = math.random() * 4 - 2, Radius = math.random() * 2 + 2, Speed = math.random() * 2 + 1}) end end local currentFolder = char:FindFirstChild("TWKS_Fireflies") if currentFolder then for i, fData in ipairs(Fireflies) do if fData.Part and fData.Part.Parent then fData.Part.Color = color local angle = tick() * fData.Speed + fData.Offset local targetX = torso.Position.X + math.cos(angle) * fData.Radius local targetZ = torso.Position.Z + math.sin(angle) * fData.Radius local targetY = torso.Position.Y + fData.Height + math.sin(tick() + fData.Offset) * 0.5 fData.Part.Position = Vector3.new(targetX, targetY, targetZ) end end end end local function UpdateLantern(char, color) local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local folder = char:FindFirstChild("TWKS_LanternFolder") if not folder then folder = Instance.new("Folder") folder.Name = "TWKS_LanternFolder" folder.Parent = char local rope = Instance.new("Part") rope.Name = "RopePart" rope.Size = Vector3.new(0.05, 0.4, 0.05) rope.CanCollide = false rope.Material = Enum.Material.SmoothPlastic rope.Color = Color3.fromRGB(50, 50, 50) rope.Parent = folder local lantern = Instance.new("Part") lantern.Name = "LanternPart" lantern.Size = Vector3.new(0.4, 0.6, 0.4) lantern.CanCollide = false lantern.Material = Enum.Material.Neon lantern.Parent = folder local light = Instance.new("PointLight") light.Range = 10 light.Brightness = 2 light.Parent = lantern local m1 = Instance.new("Motor", rope) m1.Name = "Joint1" m1.Part0 = root m1.Part1 = rope m1.C0 = CFrame.new(0.75, -0.2, 0.1) m1.C1 = CFrame.new(0, 0.2, 0) local m2 = Instance.new("Motor", lantern) m2.Name = "Joint2" m2.Part0 = rope m2.Part1 = lantern m2.C0 = CFrame.new(0, -0.2, 0) m2.C1 = CFrame.new(0, 0.3, 0) end local rPart = folder:FindFirstChild("RopePart") local lPart = folder:FindFirstChild("LanternPart") if rPart and lPart then lPart.Color = color local light = lPart:FindFirstChildOfClass("PointLight") if light then light.Color = color end local j1 = rPart:FindFirstChild("Joint1") local j2 = lPart:FindFirstChild("Joint2") local targetVelocity = root.AssemblyLinearVelocity.Magnitude LanternVelocityLerp = LanternVelocityLerp + (targetVelocity - LanternVelocityLerp) * 0.06 if j1 and j2 then local swayX = math.sin(tick() * 5) * 4 local swayZ = math.sin(tick() * 7) * 3 + (LanternVelocityLerp * 1.5) j1.C0 = CFrame.new(0.75, -0.2, 0.1) * CFrame.Angles(math.rad(swayZ), 0, math.rad(swayX)) j2.C0 = CFrame.new(0, -0.2, 0) * CFrame.Angles(math.rad(swayZ * 0.5), 0, math.rad(swayX * 0.5)) end end end Connections.Render = RunService.RenderStepped:Connect(function() pcall(function() pcall(function() if FovCircle then FovCircle.Visible = FovVisible if FovVisible then local mouseLocation = UserInputService:GetMouseLocation() FovCircle.Position = Vector2.new(mouseLocation.X, mouseLocation.Y) end end end) local rainbowColor = Color3.fromHSV((tick() % 4) / 4, 1, 1) if NotifText and NotifText.Parent then NotifText.TextColor3 = rainbowColor end if NotifStroke and NotifStroke.Parent then NotifStroke.Color = rainbowColor end if LoadingFill and LoadingFill.Parent then LoadingFill.BackgroundColor3 = rainbowColor end if DummyLine then DummyLine.BackgroundColor3 = rainbowColor end if RainbowOverlay then CustomOverlayColor = rainbowColor Line.BackgroundColor3 = rainbowColor pcall(function() if FovCircle then FovCircle.Color = rainbowColor end end) SliderFill.BackgroundColor3 = rainbowColor if SmoothAim then SmoothBtn.TextColor3 = rainbowColor end if TeamCheck then TeamBtn.TextColor3 = rainbowColor end if WallCheck then WallBtn.TextColor3 = rainbowColor end if ChamsEnabled then ChamsBtn.TextColor3 = rainbowColor end if RainbowChams then ColorModeBtn.TextColor3 = rainbowColor end OverlayColorBtn.TextColor3 = rainbowColor if CapeEnabled then CapeBtn.TextColor3 = rainbowColor end if FirefliesEnabled then FirefliesBtn.TextColor3 = rainbowColor end if LanternEnabled then LanternBtn.TextColor3 = rainbowColor end if AtmosphereMode ~= 0 then AtmosBtn.TextColor3 = rainbowColor end updateTargetUI() end if CapeEnabled and LocalPlayer.Character then DrawCape(LocalPlayer.Character, rainbowColor) else if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_CapeFolder") if f then f:Destroy() end end end if FirefliesEnabled and LocalPlayer.Character then UpdateFireflies(LocalPlayer.Character, rainbowColor) else if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_Fireflies") if f then f:Destroy() end end Fireflies = {} end if LanternEnabled and LocalPlayer.Character then UpdateLantern(LocalPlayer.Character, rainbowColor) else if LocalPlayer.Character then local f = LocalPlayer.Character:FindFirstChild("TWKS_LanternFolder") if f then f:Destroy() end end end for _, p in ipairs(Players:GetPlayers()) do if p.Character then local highlight = p.Character:FindFirstChild("TWKS_Highlight") if highlight then if ChamsEnabled and (not TeamCheck or p.Team ~= LocalPlayer.Team) then highlight.Enabled = true highlight.FillColor = RainbowChams and rainbowColor or CustomChamsColor highlight.OutlineColor = RainbowChams and rainbowColor or CustomChamsColor else highlight.Enabled = false end end local billboard = p.Character:FindFirstChild("TWKS_NameESP") if billboard then billboard.Enabled = (NamesEnabled and (not TeamCheck or p.Team ~= LocalPlayer.Team)) end end end if Aiming then local target = getClosestPlayer() if target and target.Character then local actualPart = nil local mode = AimPart if mode == "Random" then local parts = {"Head", "Torso", "LeftArm", "RightArm"} mode = parts[math.random(1, 4)] end if mode == "Head" then actualPart = target.Character:FindFirstChild("Head") elseif mode == "Torso" then actualPart = target.Character:FindFirstChild("UpperTorso") or target.Character:FindFirstChild("Torso") or target.Character:FindFirstChild("HumanoidRootPart") elseif mode == "LeftArm" then actualPart = target.Character:FindFirstChild("LeftUpperArm") or target.Character:FindFirstChild("Left Arm") elseif mode == "RightArm" then actualPart = target.Character:FindFirstChild("RightUpperArm") or target.Character:FindFirstChild("Right Arm") end if actualPart then local targetCFrame = CFrame.new(Camera.CFrame.Position, actualPart.Position) if SmoothAim then Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, Smoothness) else Camera.CFrame = targetCFrame end end end end pcall(function() DummyFrame.Visible = (MenuVisible and MainFrame.Visible) end) end) end) Connections.MouseDown = Mouse.Button2Down:Connect(function() Aiming = true Status.Text = "  AIMBOT: LOCKING" Status.TextColor3 = CustomOverlayColor end) Connections.MouseUp = Mouse.Button2Up:Connect(function() Aiming = false Status.Text = "  AIMBOT: READY (RMB)" Status.TextColor3 = Color3.fromRGB(150, 150, 150) end) MainFrame.ClipsDescendants = false
